config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 15
      name: "Sustained heavy load"
scenarios:
  - flow:
      - post:
          url: "/api/auth/login"
          json:
            email: "admin@example.com"
            password: "password123"
